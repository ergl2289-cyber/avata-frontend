/**
 * App-wide launch flags.
 *
 * MOSCOW_ONLY — запуск только по Москве (город убирается из UI, всё привязано к
 * единственному городу). Сейчас выключено: работаем по городам-миллионникам РФ
 * (плоский список, без районов/пригородов). Поставить `true` вернёт режим
 * «только Москва».
 */
export const MOSCOW_ONLY = false

/** Имя единственного города (для подписей, когда действует MOSCOW_ONLY). */
export const SINGLE_CITY_NAME = 'Москва'
