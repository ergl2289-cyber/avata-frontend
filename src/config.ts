/**
 * App-wide launch flags.
 *
 * MOSCOW_ONLY — запуск только по Москве. Город убран из UI (фильтры, поиск,
 * профиль, создание объявления); город пользователя и новых объявлений
 * автоматически = единственный город в БД (Москва). Вся логика выбора города
 * сохранена — чтобы вернуть мультигород, достаточно поставить `false`.
 */
export const MOSCOW_ONLY = true

/** Имя единственного города (для подписей, пока действует MOSCOW_ONLY). */
export const SINGLE_CITY_NAME = 'Москва'
